# Research Gaps, Assumptions, and Verification Gates

Every unresolved fact, its safe default, blast radius, and the earliest phase where it gets verified. Status markers follow the session rules: `(user)`, `(verified: source)`, `[assumed: default — if wrong: impact/fallback]`.

| ID | Gap / Assumption | Status | Safe default | Blast radius if wrong | Earliest verification (phase) |
|---|---|---|---|---|---|
| G-01 | Official repo-pool page listing `micromatch/picomatch` as JS→Go/Rust, ~2,000–2,500 LOC, MIT, "original tests must pass unmodified" | **CLOSED — VERIFIED 2026-07-31**: `https://coderesurrection.com/2026/repo-pool` (fetched 2026-07-31) lists `micromatch/picomatch | JavaScript → Go,Rust | 2000-2500 | Standard (2k–8k) | MIT` | n/a (resolved) | Was mis-rated "Low": under rule 01's BYO "not already ported" clause, an unlisted repo with prior Rust ports could have been ineligible — existential risk, now settled by pool listing | Closed Phase 0 (2026-07-31) |
| G-02 | Discord-only clarifications (submission portal, adapter templates, per-pair unsafe thresholds, test-hash procedure) | `(user)` partially — official site promises templates/thresholds "at kickoff" | Assume templates arrive at kickoff; design our own adapter plan now | Medium — adapter shape affects product-surface decision (D-001) | Phase 0/1: team reads Discord; update context.md + D-001 |
| G-03 | Exact submission deadline instant | `(verified: coderesurrection.com/2026)` Aug 03 18:00 UTC code freeze; Unstop says 02:00 PM EDT same day (consistent) | 2026-08-03 18:00 UTC | Low | Phase 0 |
| G-04 | Product surface: pure Rust library + differential CLI, no N-API/JS-runtime binding in core | **CLOSED — DECIDED 2026-07-31**: D-001 Accepted (pool listing + event FAQ thin-adapter model + team instruction) | Library + CLI runner for differential/adapter tests | n/a (resolved) | Closed Phase 0 (2026-07-31) |
| G-05 | Exact JS-regex-source parity (`makeRe(...).source` string equality) achievable | `(verified: regex crate lacks lookaround)` — **not achievable** for lookaround-bearing sources without custom engine | Scope boundary: behavioral (match-result) parity is MUST; regex-source parity is STRETCH and only defined after normalization (see spec §5.4) | Medium — if judges diff makeRe sources: mitigated by documenting impossibility + offering normalized comparison | Phase 1 (freeze corpus defines normalized comparison) |
| G-06 | `parse()` token/state object exact-shape parity | **CLOSED — DECIDED 2026-07-31**: shape parity for documented data fields; function-valued fields (`peek`/`advance`) excluded (DV-4). Evidence: full read of `test/api.picomatch.js` — token assertions use only `[type,value]` and `{output,value}` data pairs; no function-field assertions exist in the suite | Port tokens as data; skip function fields | n/a (resolved) | Closed Phase 0/1 boundary (2026-07-31) |
| G-07 | `maxExtglobRecursion` semantics when branches mix combinable and non-combinable | `(verified: lib/parse.js analyzeRepeatedExtglob)` but intricate | Port function 1:1 with unit tests from test/options.maxExtglobRecursion.js | Medium — security-relevant | Phase 7 |
| G-08 | Issue #175 (`+(ab|abab)` bypass) present in baseline | `(verified: bounded harness tools/research/redos-timing.js 2026-07-31 — not literalized; median 147.6 ms @ len 71, super-linear curve)` | Port reproduces baseline behavior (literalize-or-not must MATCH oracle), and separately document as Bug Catcher candidate | Low for parity; High if we "fix" it silently (would diverge from oracle) | Phase 9 (fuzz) + DECISIONS.md |
| G-09 | Whether `windows` auto-detect should exist in Rust library | **CLOSED — DECIDED 2026-07-31**: mirror the index.js/posix.js split exactly — `picomatch::new` (detect when `windows` is None, mirroring index.js) + `picomatch::posix` (never detects). Evidence: ARCHITECTURE.md §5, docs/api-compatibility.md §entry modes, `index.js:6-13` (detect only when option null/undefined, options object never mutated) | Mirror index.js vs posix.js split as two constructors | n/a (resolved) | Closed 2026-07-31 |
| G-10 | `nocase` Unicode fold parity (JS `i` flag vs regex crate simple folding) | `(verified: both use simple case folding per UNICODE.md RL1.5 and JS spec)` | Assume equivalence for ASCII + common BMP cases; corpus includes non-ASCII fold cases | Low-Medium (exotic folds) | Phase 9 differential fuzz with Unicode seeds |
| G-11 | Test count stability: 1977 tests on Node 24 | `(verified: npm run mocha 2026-07-31)` | Pin corpus to this run; re-run on Node 12/18/22 in CI to detect version-sensitive expectations | Low | Phase 1 |
| G-12 | Flaky/platform-skipped assertions | `(verified: test/malicious.js skips 1 assertion on win32)` | Record per-OS expectation matrix in corpus metadata | Low | Phase 1 |
| G-13 | Rust toolchain availability on all team machines + judges' machines | `(verified: NOT installed on this machine 2026-07-31)` | rust-toolchain.toml pins 1.97.1 (D-012); README documents `rustup` one-liner | Medium — build instructions must work from nothing | Phase 2 |
| G-14 | `regex` crate current MSRV vs pinned toolchain | **CLOSED — VERIFIED 2026-07-31**: registry `rust_version` fields — regex 1.13.1 → 1.65, fancy-regex 0.19.0 → 1.66, both far below the 1.97.1 pin (crates.io API) | n/a (verified at adoption again via Cargo.lock) | n/a | Closed Phase 0 (2026-07-31); re-checked at Phase 2 adoption |
| G-15 | `fancy-regex` worst-case backtracking exposure | `(verified: docs — exponential possible for fancy features)` | Wrap fancy-regex calls in a step/time guard; risky-extglob literalization already removes worst cases; document residual risk | Medium (DoS on untrusted patterns) | Phase 9 adversarial bench |
| G-16 | `.port-mortem.toml` exact schema | `(user)` — official anatomy shows file with track letter, source URL, kickoff hash | Create with those three fields + repo URL; adjust when template published | Low | Phase 11 |
| G-17 | Event Write-Up side quest vs Unstop "Community Choice" | `(verified: official site)` Write-Up replaces Community Choice; Unstop stale | Follow official site | None (informational) | — |
| G-18 | Whether judges run tests on Linux, Windows, or macOS | `[assumed: Linux primary — if wrong: CI covers all three from Phase 11]` | CI matrix all three | Low | Phase 11 |
