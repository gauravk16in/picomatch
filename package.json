{
  "name": "pmx-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "picomatch → Rust port workspace. JS-side harness only; the port itself is Rust (crates/).",
  "scripts": {
    "parity": "node fixtures/parity-run.js",
    "test:rust": "cargo test --workspace"
  }
}
