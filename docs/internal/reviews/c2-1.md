# review: C2 — iteration 1

VERDICT: ACCEPT — 0 BLOCKER, 0 MAJOR, 0 MINOR.

Chunk C2 (slash + dot segment semantics) is fully implemented, verified, and accepted.

## 1. Checks 1–7 Summary

1. **Rule-by-rule vs design**:
   - `slash_branch` implements L975-L991: leading `./` collapse (advances `start`, clears `consumed` and `output`, pops `dot` token, resets `prev` to `bos`).
   - `dot_branch` implements L997-L1015 (excluding C5 brace dots hook L998-L1006): differentiates between `TokenKind::Text` (inside text segments, merged via `push()`) and `TokenKind::Dot` (BOS/slash adjacent).
2. **Citation truth**:
   - `main_loop.rs:97-117` citations to `parse.js:L975-L991` verified.
   - `main_loop.rs:119-137` citations to `parse.js:L997-L1015` verified.
3. **Rustic escape hatches**:
   - Grep in `crates/pmx-core/src`: 0 hits for `unsafe`, `unwrap`, `expect`, `panic`, `todo`, or narrowing `as` casts.
4. **Oracle & unit tests**:
   - `verify-c0`: 39/39 deterministic.
   - `verify-c1`: 55/55 deterministic.
   - `verify-c2`: 18/18 deterministic.
   - `cargo test`: 6 unit tests + 2 corpus runner tests pass 100%.
5. **Staging honesty & chunk boundaries**:
   - C2 staged rows `text.merge` (c0), `sl.escape.dropdot` (c1), and `sl.text.astral-merge` (c1) activated cleanly.
   - Unbuilt branches (C3-C9) remain staged without premature logic.
6. **Adversarial differential testing**:
   - `fixtures/attack-c2.js`: 85 adversarial cases covering combinations of `.`, `/`, `./`, `..`, `...`, `a/./b`, `a/../b`, `a//b`, `a.b`, `a/b`, `a\\.b`, `a\\/b`, windows mode, bash mode, unescape.
   - Result: 85/85 0 divergences.
7. **VCS & commit readiness**:
   - Clean branch `feat/c2-segment-semantics`.

## 2. Ledger Update

Recorded in `.loop/review-ledger.md`.
