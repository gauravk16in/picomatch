# review: C3 — iteration 1

VERDICT: ACCEPT — 0 BLOCKER, 0 MAJOR, 0 MINOR.

Chunk C3 (single wildcards `?`, `*`, and `+` with BOS/dot guards) is fully implemented, verified, and accepted.

## 1. Checks 1–7 Summary

1. **Rule-by-rule vs design**:
   - `qmark_branch` implements `lib/parse.js:L1021-L1047`: paren lookaround/group disambiguation (`\?` vs raw `?`), `QMARK_NO_DOT` at BOS/slash unless `opts.dot`, `QMARK` elsewhere.
   - `star_branch` implements `lib/parse.js:L1246-L1283`: `opts.bash` mode (`.*?` with optional `nodot`), `opts.regex` mode passthrough after bracket/paren, BOS/slash/dot guard prefix emission (`nodot`, `NO_DOTS_SLASH`, `NO_DOT_SLASH` + `ONE_CHAR` when next char != `*`), `state.output` and `prev.output` prefix mutation.
   - `plus_branch` implements `lib/parse.js:L1071-L1089`: `PLUS_LITERAL` (`\+`) emission at BOS/default vs raw `+` when inside bracket/paren/brace or `parens > 0`.
2. **Citation truth**:
   - `main_loop.rs` citations to `parse.js:L1021-L1047`, `L1071-L1089`, and `L1246-L1283` verified.
3. **Rustic escape hatches**:
   - Grep in `crates/pmx-core/src`: 0 hits for `unsafe`, `unwrap`, `expect`, `panic`, `todo`, or narrowing `as` casts.
4. **Oracle & unit tests**:
   - `verify-c0`: 39/39 deterministic.
   - `verify-c1`: 55/55 deterministic.
   - `verify-c2`: 487/487 deterministic.
   - `verify-c3`: 521/521 deterministic.
   - `cargo test`: 8 unit tests + 2 corpus runner tests pass 100%.
5. **Staging honesty & chunk boundaries**:
   - C3 active rows activated cleanly across `c0_oracle.json`, `c1_oracle.json`, `c2_oracle.json`, and `c3_oracle.json`.
   - Unbuilt branches (C4-C9) remain staged without premature logic.
6. **Adversarial differential testing**:
   - `fixtures/attack-c3.js`: 80 adversarial cases covering combinations of `?`, `*`, `+`, `(?=a)`, `(?!a)`, `(?<=a)`, `(?<!a)`, `(?<name>a)`, `{dot, bash, regex, windows, capture, strictSlashes}` options.
   - Result: 80/80 0 divergences.
7. **VCS & commit readiness**:
   - Clean branch `c2-segment-semantics`.

## 2. Ledger Update

Recorded in `.loop/review-ledger.md`.
