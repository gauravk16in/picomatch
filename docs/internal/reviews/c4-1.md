# review: C4 — iteration 1

VERDICT: ACCEPT — 0 BLOCKER, 0 MAJOR, 0 MINOR.

Chunk C4 (Fastpath gallery `parse.fastpaths`) is fully implemented, verified, and accepted.

## 1. Checks 1–7 Summary

1. **Rule-by-rule vs design**:
   - `fastpaths` implements `lib/parse.js:L1324-L1414`: options coercion (`max_length`), UTF-16 length guard (`InputTooLong`), pre-substitution (`constants::replacement`), prefix removal (`utils::remove_prefix`).
   - Platform template creation (`create(str)`): 8 fixed cases (`*`, `.*`, `*.*`, `*/*`, `**`, `**/*`, `**/*.*`, `**/.*`), extension chain fallback (`/^(.*?)\.(\w+)$/`), `nodot`/`slashDot` selection, `globstar()` helper (with `noglobstar` fallback), and `strictSlashes !== true` trailing slash suffix.
2. **Citation truth**:
   - `fastpath.rs` citations to `parse.js:L1324-L1414` verified.
3. **Rustic escape hatches**:
   - Grep in `crates/pmx-core/src/parse/fastpath.rs`: 0 hits for `unsafe`, `unwrap`, `expect`, `panic`, `todo`.
4. **Oracle & unit tests**:
   - `verify-c4`: 385/385 deterministic.
   - `cargo test`: 11 unit tests + 2 corpus runner tests pass 100%.
5. **Adversarial differential testing**:
   - `fixtures/attack-c4.js`: 286 adversarial cases covering combinations of fastpath patterns and option flags (`dot`, `bash`, `capture`, `noglobstar`, `strictSlashes`, `windows`, `maxLength`).
   - Result: 286/286 0 divergences.
6. **VCS & commit readiness**:
   - Clean branch `c2-segment-semantics`.

## 2. Ledger Update

Recorded in `.loop/review-ledger.md`.
