# tests/differential/ — Replay Drivers (Phase 4+)

Rust-side differential replay drivers + CLI protocol conformance tests live here from Phase 4 onward. Inputs come from `tests/corpus/` (generated by the Node oracle, dev-only); nothing here may hand-write expectations without an oracle citation. See `docs/differential-testing.md` for the protocol and triage rules.
